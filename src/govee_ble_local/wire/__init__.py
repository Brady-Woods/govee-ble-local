"""Wire layer: spec-first frame build/parse/reassemble (v3).

Parsing goes through the shipped Kaitai reader (``govee_ble_local._generated``);
builders are spec-aligned and round-trip-proven against that reader.
"""
